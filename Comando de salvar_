<<<<<<< HEAD
git config user.name "labevofern"
git config user.email "labevol.samambaias@ufpe.br"

# 1. Adiciona todos os arquivos modificados e novos para a "área de preparação" (staging area).
# O Git agora sabe quais alterações você quer salvar no próximo commit.
git add .

# NOTA SOBRE AVISOS (WARNINGS):
# Ao executar "git add .", você pode ver avisos como "warning: LF will be replaced by CRLF".
# Isso é normal e esperado no Windows. Significa que o Git está ajustando as quebras de linha
# dos arquivos para o padrão do Windows. Não é um erro e pode ser ignorado.
# Seus arquivos foram adicionados com sucesso. Prossiga para o próximo comando.

# 2. Salva as alterações que estão na "área de preparação" em um commit (um "snapshot" do seu projeto).
# A mensagem (-m) descreve o que foi feito. "Minhas edições no site" é uma boa mensagem.
git commit -m "Minhas edições no site"

# 3. Envia os seus commits locais para o repositório remoto no GitHub.
# Como você configurou o usuário local, este commit será associado à conta do laboratório.
# O Git pode pedir o nome de usuário e senha/token da conta do GitHub do laboratório na primeira vez.
git push origin main
